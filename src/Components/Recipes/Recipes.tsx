import ss from './Recipes.module.scss';

const Recipes: React.FC = () => {
    return (
        <div className={ss.recipes}>
            <h1>Admin Access Required</h1>
        </div>
    );
};

export default Recipes;
